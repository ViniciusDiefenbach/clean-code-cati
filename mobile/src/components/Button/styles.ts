import { StyleSheet } from 'react-native'
import { backgroundColor2 } from '../../../config/app-colors'

export const styles = StyleSheet.create({
  button: {
    backgroundColor: backgroundColor2,
    borderRadius: 8,
    paddingVertical: 16,
    paddingHorizontal: 12,
    alignItems: 'center',
    justifyContent: 'center'
  }
})
