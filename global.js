import { StyleSheet } from "react-native";
import { backgroundColor1 } from "./config/css-variables";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: backgroundColor1,
    alignItems: "center",
    justifyContent: "center",
  },
});
